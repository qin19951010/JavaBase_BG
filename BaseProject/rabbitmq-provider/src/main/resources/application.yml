server:
  port: 8021

spring:
  #给项目取个名字
  application:
    name: rabbitmq-provider
    #配置rabbitMq 服务器
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest

    #确认消息已发送到队列(Queue)
    publisher-confirm-type: correlated